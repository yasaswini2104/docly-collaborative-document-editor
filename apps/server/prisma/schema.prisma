// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// ── Models will be added in subsequent modules ─────────────────────────────
//
// Placeholder comment block only — no business-logic models are defined here.
// The User and Document models will be introduced in the Auth and Document
// modules respectively.
